# 生成的图片文件（二进制，体积大，不纳入版本控制）
outputs/images/

# 本地敏感配置（含 API Key、已授权命令等，绝不上传）
settings.local.json

# github-config.sh 通过 push 脚本脱敏后上传，git skip-worktree 保护本地真实值
# skills/nano-banana-generation/references/api-config.md 同理
